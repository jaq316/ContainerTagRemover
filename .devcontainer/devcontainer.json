{
  "tasks": {
    "test": "cd tests/ContainerTagRemover.Tests && dotnet restore && dotnet test",
    "build": "cd src/ContainerTagRemover && dotnet build",
    "launch": "dotnet run --project src/ContainerTagRemover/ContainerTagRemover.csproj -- <registry> <repository> <config-file>"
  }
}